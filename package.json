{
  "name": "explore-formal-js-targets",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Comparing Dafny / Agda / Idris2 JavaScript compile targets",
  "scripts": {
    "build": "node generator/build.js",
    "ses": "node generator/ses-check.js",
    "ffi": "node generator/ffi-probe.js",
    "bundle": "node generator/bundle.mjs",
    "report": "node generator/report.js",
    "all": "node generator/build.js && node generator/ses-check.js && node generator/ffi-probe.js && node generator/bundle.mjs && node generator/report.js",
    "check": "npm run all && git diff --exit-code -- docs/ || (echo '\n*** docs/ are out of date. Run `npm run all` and commit the regenerated files. ***' && exit 1)"
  },
  "dependencies": {
    "@endo/bundle-source": "^4.3.0",
    "@endo/import-bundle": "^1.6.1",
    "bignumber.js": "^9.1.2",
    "ses": "^1.10.0"
  }
}
